const fruits = ['apple', 'mango', 'pear', 'grapes'];

const arrFilter = (arr) => {
  return arr.filter((element) => element.includes('ea'));
};

console.log(arrFilter(fruits));

/**
 * foreach
 * everyRR
 * * fiter
 * reduce
 * map
 * * sort
 * sort(a,b)
 */
